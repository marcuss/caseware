# The tests deliberately break the worker's collaborators, so the worker logs what it survived.
# Those logs are the point of the code under test, not of the build output: assertions cover them.
handlers = java.util.logging.ConsoleHandler
.level = INFO
com.caseware.fanout.level = OFF
